[project]
name = "voise"
version = "0.1.0"
description = "Python client for the Voise AI voice agent platform API"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[project.urls]
Homepage = "https://github.com/hameedsk381/voice-agents"
