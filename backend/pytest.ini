[pytest]
# Collect only the unit/integration test suite. Root-level files like
# test_smart_agent.py and test_agents_direct.py are manual scripts that hit a
# live DB / websocket and are not part of the automated suite.
testpaths = tests
asyncio_mode = strict
