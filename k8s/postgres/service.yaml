apiVersion: v1
kind: Service
metadata:
  name: voise-postgres
  namespace: voise
  labels:
    app.kubernetes.io/name: voise-postgres
    app.kubernetes.io/part-of: voise
spec:
  selector:
    app: voise-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: pgql
  clusterIP: None
