apiVersion: v1
kind: Service
metadata:
  name: voise-grafana
  namespace: voise
  labels:
    app.kubernetes.io/name: voise-grafana
    app.kubernetes.io/part-of: voise
spec:
  selector:
    app: voise-grafana
  ports:
    - port: 3000
      targetPort: 3000
      name: http
